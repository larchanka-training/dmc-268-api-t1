"""Порт вложений: что слою приложения позволено знать об эмбеддингах.

Батчевый интерфейс нарочно один: retrieval и накопление профилей всегда
переводят список текстов в список векторов, а не по одному. Ни модели, ни
размерности здесь нет — они конфигурация вызывающего, а не знание порта.
"""

from collections.abc import Sequence
from typing import Protocol


class EmbeddingGateway(Protocol):
    def embed(self, texts: Sequence[str]) -> list[list[float]]:
        """Перевести тексты в векторы той же длины и в том же порядке."""
        ...
