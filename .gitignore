__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
.env
.pytest_cache/
.ruff_cache/
.idea/
.DS_Store
CLAUDE.md

.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# AI agent skills and rules are the DevTools / Agentic Engineer ticket's
# deliverable, not this one's. Until that lands, generate your own with
# `openspec init --tools <your-tool>` and keep it local.
.agents/
.claude/
.cursor/
.codex/
.gemini/
.windsurf/

# Build output from an editable install
*.egg-info/
build/
dist/
