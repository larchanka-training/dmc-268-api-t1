# Tasks

## 1. Классификация секрета

- [x] 1.1 Написать чистую функцию `classify_secret_kind` (shell, вход — значение на stdin, выход — `key` или `password`, без сети и без печати входа) в `.github/workflows/vps-access-probe.yml`; проверить локально, прогнав её на синтетическом ключе (строка с префиксом `-----BEGIN`) и на произвольной строке — вывод `key` и `password` соответственно, само значение в выводе не появляется
- [x] 1.2 Передать `VPS_DMC268_P` в шаг через `env:`, а не интерполяцией в тело скрипта; проверить, что в собранном YAML нет `${{ secrets.VPS_DMC268_P }}` внутри `run:`

## 2. Адаптеры подключения

- [x] 2.1 Добавить шаг подготовки `known_hosts` через `ssh-keyscan` по `VPS_DMC268_IP_T1`; проверить, что при пустом ответе keyscan шаг сообщает «хост недоступен» и дальше не идёт
- [ ] 2.2 Адаптер входа по ключу: записать значение во временный файл с правами `600`, подключиться с `-o BatchMode=yes -o ConnectTimeout=10`; проверить, что временный файл удаляется в том же шаге независимо от исхода
- [x] 2.3 Адаптер входа по паролю: `sshpass -e` (значение из `SSHPASS`, не аргументом), та же одна попытка с тем же таймаутом; проверить через `ps` в соседнем шаге, что значение не видно в командной строке
- [x] 2.4 Ограничить прогон одной попыткой входа: без матрицы, без `continue-on-error` на ретрай, без повторного шага; проверить по логу успешного прогона, что строк подключения ровно одна

## 3. Проверка Docker на сервере

- [x] 3.1 Выполнить по установленному соединению `docker version --format '{{.Server.Version}}'`; проверить, что шаг различает три исхода — версия получена, демон не отвечает, отказ в доступе к сокету — и не выполняет ничего, кроме этой команды

## 4. Отчёт

- [x] 4.1 Записать в `$GITHUB_STEP_SUMMARY` три факта: форма секрета, результат SSH, результат Docker; проверить, что summary читается без открытия логов
- [ ] 4.2 Сделать отрицательный ответ штатным исходом: прогон зелёный и при «SSH: отказ», красный — только при ошибке самого workflow; проверить, подсунув заведомо недоступный адрес через `workflow_dispatch`-вход

## 5. Прогон и фиксация результата

- [x] 5.1 Запушить ветку и дождаться автоматического прогона по `paths`; приложить ссылку на run в [#22](https://github.com/larchanka-training/dmc-268-api-t1/issues/22)
- [x] 5.2 Записать полученные три факта в #22 и, если `VPS_DMC268_P` оказался паролем, завести там же пункт про bootstrap-шаг с установкой ключа CI
- [ ] 5.3 Удалить workflow-файл отдельным коммитом после фиксации результата; проверить, что в `.github/workflows/` остался только `ci.yml`

## Примечания к выполнению

- **2.2 не отмечена:** ветка входа по ключу написана, но ни разу не выполнялась — секрет оказался паролем, и прогон ушёл в парольную ветку. Проверять её отдельным прогоном смысла нет: она удаляется вместе с файлом.
- **4.2 не отмечена:** отрицательный исход не воспроизводился. И `ssh-keyscan`, и вход удались с первого раза, а `workflow_dispatch` с подменой адреса недоступен, пока файл не в ветке по умолчанию. Тратить на это отдельный прогон не стал.
- **5.3 отложена осознанно:** диагностика показала, что Docker на сервере не установлен, поэтому после его установки прогон понадобится повторить. Удалять файл до этого преждевременно.
