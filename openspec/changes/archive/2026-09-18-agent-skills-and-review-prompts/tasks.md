## 1. Правила разработки

- [x] 1.1 Создать `.agents/rules/backend.md`: стек, менеджер пакетов, команды проверок и тестов
      дословно, вход в архитектуру по D9 (`openspec/specs/` как контракт,
      `docs/BACKEND_ARCHITECTURE.md` как объяснение, `docs/SYSTEM_DESIGN.md` как межсервисные
      границы), контракты `tool.importlinter` ссылкой на `pyproject.toml`, запрет менять
      поведение в обход OpenSpec-change; проверка — файл существует, каждая названная команда
      выполняется в чистом клоне, объём в пределах 200–250 строк
- [x] 1.6 Добавить в `.agents/rules/backend.md` раздел по D16: определение шва (место, где
      поведение подменяется без правки этого места; у нас совпадает с границами слоёв и
      портами), тест на удаление, правило «один адаптер — шов гипотетический, два — настоящий»
      и таблицу категорий зависимостей (чистое вычисление / локально подменяемое / своё по сети
      / чужое внешнее) с ответом, нужен ли порт; проверка — по таблице разбирается каждый порт
      из раздела «Порты» в `docs/BACKEND_ARCHITECTURE.md` и вывод совпадает с тем, что в
      репозитории реализовано
- [x] 1.2 Создать `.agents/rules/git-and-pr.md`: git flow — ветка отводится от `develop` и
      мёржится в `develop`, `main` получает только прошедшее через `develop`; имена веток
      `fix/…`, `feat/…`, `deps-update-YYYY-MM-DD`, conventional commits, обязательные заголовок
      и содержательное описание пул-реквеста с объяснением, что это первый контекст для
      ревью-бота; разбивка задачи на два стека по D12 — родительский issue с постановкой и DoD
      плюс sub-issues `— backend` и `— frontend`, каждая со своим пул-реквестом, родитель
      закрывается последним; обязательная привязка пул-реквеста к задаче — `Closes #N` в своём
      репозитории и панель Development (мутация `addCloseIssueReferences`) в чужом, с готовой
      командой `gh`; правило D13 «всё через `gh`, при отсутствии подкоманды — `gh api` или
      `gh api graphql`, веб только последним» с рецептами для подзадач и привязки; работа с
      замечаниями — три исхода треда по D15 и перезапрос ревью у автора замечаний после пачки
      правок; проверка — имена веток и формат коммитов в файле
      совпадают с историей `develop`, база открытых пул-реквестов — с описанным потоком, а каждая
      названная команда `gh` выполняется как есть
- [x] 1.3 Переписать `AGENTS.md` по D6: критичные правила текстом, детали ссылками, объём в
      пределах полусотни строк; проверка — каждая названная в файле команда выполняется в чистом
      клоне, и по файлу видно, где лежат требования, архитектура и доска
- [x] 1.4 Дополнить `AGENTS.md` ссылками на `.agents/rules/`, `.agents/skills/` и
      `prompts/review/`, когда эти каталоги появятся; проверка — ни одна ссылка в файле не ведёт
      в несуществующий путь
- [x] 1.5 Создать `.agents/README.md`: что лежит в каталоге, как инструменты его подключают,
      почему выбран `.agents/`, а не `docs/agents/`; проверка — по файлу восстанавливается
      раскладка без чтения тикета

## 2. Скиллы под наш стек

- [x] 2.1 Создать скилл `.agents/skills/backend-tdd/SKILL.md` по D14: швы по слоям (чистая
      функция домена, сценарий через порт, адаптер через репозиторий, API через `TestClient`),
      правила витка «красный до зелёного, один шов, рефакторинг относится к ревью»,
      антипаттерны (прибитый к реализации, тавтологический, горизонтальная нарезка), моки только
      на границе системы, база настоящая через `TEST_DATABASE_URL`, правило «заменяй, не
      наслаивай» (после углубления модуля тесты поверх его частей удаляются), запрет на молча
      пропущенный тест; ссылка на источник (`mattpocock/skills`, MIT); проверка — скилл называет реальные
      команды pytest и маркер `integration` из `pyproject.toml`
- [x] 2.6 Вынести примеры скилла в соседние файлы `tests.md` (пары «хороший / плохой тест» на
      коде этого репозитория) и `mocking.md` (граница мока, инъекция времени и идентификаторов);
      проверка — каждый пример запускается из репозитория и проходит, а `SKILL.md` остаётся
      короче сотни строк
- [x] 2.2 Создать скилл `.agents/skills/backend-code-review/SKILL.md` по D10: фиксация точки
      отсчёта через `git diff <база>...HEAD` от `develop`, две параллельные оси Standards и Spec,
      источники Standards — `AGENTS.md`, `.agents/rules/backend.md`,
      `docs/BACKEND_ARCHITECTURE.md`, контракты `tool.importlinter`, глубина интерфейса и тест
      на удаление из D16; источник Spec — артефакты
      OpenSpec-change и issue через `gh issue view`; базовый набор запахов Фаулера как эвристики;
      запрет на замечания, которые доказывают `ruff` и `lint-imports`; отчёты по осям не
      сливаются; ссылка на источник (`mattpocock/skills`, MIT) в теле скилла; проверка — прогон
      на диффе `develop...HEAD` даёт два раздельных отчёта, и ни одно замечание не дублирует
      вывод `uv run ruff check .` и `uv run lint-imports`
- [x] 2.3 Создать скилл `.agents/skills/pull-request/SKILL.md` по D11: тело из разделов
      Summary (наименьшее изображение сути — псевдокод, дерево вызовов или файлов, mermaid,
      `diff`), Evidence (какой тест падал и какой проходит) и Merge Danger (дверь в одну или обе
      стороны, радиус поражения, для миграций — состояние `downgrade`); механика — проверка
      `gh auth status`, поиск шаблона в `.github/`, обновление существующего пул-реквеста вместо
      второго, передача описания через `--body-file`, база `develop`, `Closes #N`; ссылки на
      источники (`mattpocock/skills` и `humanlayer/skills`, MIT) в теле скилла; проверка —
      описание, собранное скиллом для текущей ветки, содержит три раздела и проходит требования
      `.agents/rules/git-and-pr.md`, а открытый пул-реквест привязан к своей задаче и эта
      привязка видна в ленте задачи
- [x] 2.5 Добавить в тот же скилл режим работы с замечаниями по D15: вытащить треды через `gh`,
      разложить на три исхода (нужна правка — правим, отвечаем и резолвим сами; вопрос без
      правки — отвечаем и не резолвим; несогласие — аргумент и открытый тред), после всех правок
      перезапросить ревью у автора замечаний одним запросом; проверка — на пул-реквесте с
      замечаниями режим не резолвит ни одного треда без ответа, не резолвит треды-вопросы и
      оставляет ровно один перезапрос ревью
- [x] 2.4 Создать скилл `.agents/skills/alembic-migration/SKILL.md`: генерация ревизии, проверка
      одной головы, обратимость, рецепт `ALTER TYPE` для расширения enum; проверка — шаги скилла
      совпадают с разделом «Миграции» в `docs/BACKEND_ARCHITECTURE.md` и проходят на пустой базе

## 3. Шаблоны кода и тестов

- [x] 3.1 Создать `.agents/templates/backend/pure-function.md` — чистая функция домена и её
      юнит-тест без базы; проверка — код из шаблона проходит `uv run ruff check .` и
      `uv run pytest` после подстановки имён
- [x] 3.2 Создать `.agents/templates/backend/adapter.md` — адаптер инфраструктуры и
      интеграционный тест с маркером `integration`; проверка — тест из шаблона пропускается без
      `TEST_DATABASE_URL` и проходит с ним
- [x] 3.3 Создать `.agents/templates/backend/endpoint.md` — роутер, схема запроса и ответа, тест
      через `TestClient`; проверка — код из шаблона не нарушает `uv run lint-imports`

## 4. Системные промпты AI-ревью

- [x] 4.1 Создать `prompts/review/system.md` — главный системный промпт: порядок анализа
      безопасность → корректность → производительность → читаемость, запрет замечаний про
      линтинг и форматирование, чтение `AGENTS.md` проверяемого репозитория, требование
      краткости и осторожности в утверждениях о невидимом контексте; проверка — каждый сценарий
      требований «Порядок анализа», «Чтение файла контекста» и «Краткость и осторожность» из
      `specs/ai-review-prompting/spec.md` покрыт явной строкой промпта
- [x] 4.2 Создать `prompts/review/repo-conventions.md` — промпт промежуточного шага, дающий
      артефакт с полями `files`, `key_patterns`, `recommendations`, и пример артефакта для этого
      репозитория; проверка — пример содержит конвенции, наблюдаемые в `app/`, а не общие слова
- [x] 4.3 Создать `prompts/review/custom-rules.md` — формат пользовательского правила
      (glob + исключение + имя + нумерованные проверки), три мета-инструкции (область действия,
      обязательный префикс атрибуции, граница применения префикса) и стартовый набор правил;
      проверка — сценарии требований «Формат пользовательских правил», «Область действия» и
      «Атрибуция сработавшего правила» покрыты, русская форма префикса совпадает со
      спецификацией, английский оригинал приведён как ссылка на источник формы
- [x] 4.4 Создать `prompts/review/comment-templates.md` — скелет комментария к строке и шаблон
      сводки с примером заполнения; проверка — пример проходит по всем пяти шагам скелета
- [x] 4.5 Создать `prompts/review/README.md` — как файлы собираются в один прогон, где проходит
      граница между промптом и постобработкой, что из этого обязано быть кодом, а не промптом;
      проверка — описан отсев замечаний про линтинг на обоих этапах, как требует спецификация

## 5. Защита от расхождения

- [x] 5.1 Добавить в `tests/test_docs.py` тест, проверяющий, что каждая команда `uv run …` из
      `.agents/rules/backend.md` встречается в `.github/workflows/ci.yml` или `README.md`;
      проверка — тест падает, если в правила вписать несуществующую команду, и проходит на
      текущем содержимом

## 6. Сдача

- [x] 6.1 Дополнить `README.md` ссылкой на `.agents/README.md` и `prompts/review/README.md`;
      проверка — из README виден вход в оба каталога (ссылка на доску и правило про
      `openspec/specs/` в README уже добавлены)
- [x] 6.2 Прогнать `uv run ruff check .`, `uv run lint-imports` и `uv run pytest` на ветке;
      проверка — все три команды зелёные, ни один тест не пропущен
- [x] 6.3 Открыть пул-реквест **в `develop`**, закрывающий issue #10 (`Closes #10`), отметив в
      описании, что он стоит стопкой поверх PR #5 (ветка отведена от него, потому что в
      `develop` ещё нет `docs/BACKEND_ARCHITECTURE.md`); перечислить строки, предлагаемые роли 1
      для `AGENTS.md`, и назвать отложенное под YAGNI; проверка — CI зелёный, база пул-реквеста
      `develop`, описание содержит оба списка, а привязка к #10 видна в ленте задачи
- [x] 6.4 Оставить комментарий в #10: зафиксированная раскладка каталогов, предложение для
      `AGENTS.md`, ссылка на пул-реквест, состояние фронтовой половины
      (`larchanka-training/dmc-268-ui-t1#7`); проверка — комментарий виден в тикете и закрывает
      вопрос про раскладку и про роль 1
- [x] 6.5 Перевести карточку подзадачи «AI dev tools — backend» на доске `dmc-268-t1`
      (https://github.com/orgs/larchanka-training/projects/7) из In Progress в Review при
      открытии пул-реквеста и в Done после мерджа; родительскую карточку #11 закрывать только
      после фронтовой половины; проверка — статус карточки на доске совпадает с состоянием
      пул-реквеста, родитель остаётся открытым
