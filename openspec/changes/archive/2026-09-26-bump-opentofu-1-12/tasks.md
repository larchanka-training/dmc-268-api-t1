# Tasks

## 1. Бамп

- [x] 1.1 Поднять `tofu_version` до 1.12.6 в `ci.yml`, `deploy.yml` и `tfstate-backend.yml`; проверить, что ни одного упоминания 1.10.5 не осталось
- [x] 1.2 Поднять `required_version` в `infra/versions.tf` до `>= 1.12.0` и поправить упоминание версии в `infra/README.md`; проверить, что README и конфигурация называют одну и ту же версию

## 2. Приёмка

- [x] 2.1 Дождаться зелёного job `terraform` в `ci.yml` на новой версии (`fmt -check`, `init -backend=false`, `validate`); приложить ссылку на прогон
- [x] 2.2 Прогнать деплой и убедиться, что `apply` на существующем состоянии даёт `No changes` — то есть новая версия не считает конфигурацию изменившейся
- [x] 2.3 Убедиться, что стенд продолжает отвечать по `/health` после прогона

## Примечания к выполнению

Проверки: [CI 36202140266](https://github.com/larchanka-training/dmc-268-api-t1/actions/runs/36202140266) — `terraform`-job зелёный на 1.12.6; [деплой 36202362677](https://github.com/larchanka-training/dmc-268-api-t1/actions/runs/36202362677) — повторный прогон того же коммита дал `No changes`, стенд отвечает по `/health` и снаружи.

Первый прогон после бампа показал «3 added, 3 destroyed» — это не версия, а новый commit sha в теге образа. Чистую проверку дал только перезапуск того же коммита.
