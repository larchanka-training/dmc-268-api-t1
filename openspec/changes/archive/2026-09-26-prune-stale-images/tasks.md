# Tasks

## 1. Уборка

- [x] 1.1 Добавить в `deploy.yml` шаг после успешного `apply`, удаляющий по ключу CI неиспользуемые образы старше недели и печатающий освобождённое место; проверить, что шаг помечен как не роняющий прогон
- [x] 1.2 Убедиться, что шаг не трогает образы работающих контейнеров: сверить список образов до и после на живом стенде

## 2. Приёмка

- [x] 2.1 Прогнать деплой и убедиться по логу, что уборка отработала и сообщила результат
- [x] 2.2 Проверить на сервере, что `postgres`, `rabbitmq`, `rustfs` и текущий образ API на месте, а разовый `alpine` удалён

## Примечания к выполнению

Прогон [36202750779](https://github.com/larchanka-training/dmc-268-api-t1/actions/runs/36202750779): шаг отработал и сообщил `Total reclaimed space: 0B` — и это правильный ответ, образов старше недели на стенде пока нет.

Мусор, накопленный за сегодняшнюю отладку (разовый `alpine` и два прежних тега API), убран вручную той же командой без порога: под правило «старше недели» он не подпадал. На сервере остались `postgres:18-alpine`, `rabbitmq:3.13-alpine`, `rustfs/rustfs:1.0.0` и текущий образ API.
