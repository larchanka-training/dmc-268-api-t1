# Tasks

## 1. Один механизм вместо двух

- [x] 1.1 Убрать `-i ~/.ssh/ci_key` из команд в `deploy.yml` и composite action, оставив `IdentityFile` в `~/.ssh/config`; проверить `grep`, что флага не осталось ни в одном файле
- [x] 1.2 Убедиться, что запись в `~/.ssh/config` покрывает все подключения, а не только к хосту стенда; проверить, что директива применяется к тому же адресу, к которому идут команды

## 2. Явная связь

- [x] 2.1 Дописать в шагах `deploy.yml`, которые подключаются к серверу, откуда берутся ключ и `known_hosts`; проверить, что из текста шага понятно, почему он сломается при перестановке
- [x] 2.2 Отметить то же самое в описании composite action — что он готовит подключение для всего job'а, а не только для своих шагов; проверить, что это видно тому, кто читает только action

## 3. Приёмка

- [x] 3.1 Прогнать деплой целиком и убедиться, что он проходит, стенд отвечает, а уборка образов и проверки живости отработали

## Примечания к выполнению

Приёмка: [деплой 36204322914](https://github.com/larchanka-training/dmc-268-api-t1/actions/runs/36204322914) — все подключения прошли без флага `-i`, `apply` отработал, уборка образов и обе проверки живости тоже. То есть `IdentityFile` покрывает и наши команды, и вызовы docker-провайдера.
