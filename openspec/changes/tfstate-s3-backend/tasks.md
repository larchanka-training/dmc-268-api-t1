# Tasks

## 1. Учётные данные MinIO

- [x] 1.1 Сгенерировать пару логин/пароль (пароль — не меньше 32 случайных символов) и положить секретами `S3_USER_DMC268_T1` и `S3_PASSWORD_DMC268_T1` в `dmc-268-api-t1`; проверить по `gh secret list`, что оба появились
- [x] 1.2 Положить ту же пару в `dmc-268-ui-t1`; проверить по `gh secret list` этого репозитория
- [ ] 1.3 Записать в [#22](https://github.com/larchanka-training/dmc-268-api-t1/issues/22), что учётные данные заведены в двух местах и при ротации меняются в обоих; проверить, что пункт виден в теле задачи

## 2. Подъём MinIO на сервере

- [x] 2.1 Написать чистую функцию `render_s3_script` (вход — имя контейнера, имя тома и порт; выход — текст идемпотентного скрипта; без сети и без учётных данных внутри); проверить локально, что на синтетических значениях вывод содержит проверку существования контейнера и не содержит слова `RUSTFS_SECRET_KEY`
- [x] 2.2 Передавать учётные данные запускаемому контейнеру переменными окружения через `--env-file`, читаемый из stdin, а не аргументами `docker run`; проверить, что в отрендеренном скрипте пароль не появляется в командной строке
- [ ] 2.3 Собрать job, выполняющий скрипт по ключу CI одной сессией и публикующий порт только на `127.0.0.1`; проверить прогоном, что контейнер поднялся, а повторный прогон сообщает «уже запущен»

## 3. Туннель и бакет

- [ ] 3.1 Добавить шаг подъёма SSH-туннеля к порту MinIO с явной проверкой, что локальный порт слушается; проверить, что при неподнявшемся туннеле шаг падает с понятным сообщением до обращения к хранилищу
- [ ] 3.2 Создать бакет под состояние идемпотентно через `aws s3api create-bucket` по туннелю; проверить, что повторный запуск не падает на `BucketAlreadyOwnedByYou`
- [ ] 3.3 Включить версионирование бакета и убедиться, что `aws s3api get-bucket-versioning` отвечает `Enabled`
- [ ] 3.4 Выяснить, поддерживает ли связка нашей версии `tofu` с хранилищем нативную блокировку состояния, и записать ответ в `design.md`; проверить попыткой `tofu init` с включённым `use_lockfile` и без него

## 4. Бэкенд в конфигурации

- [ ] 4.1 Добавить `infra/backend.tf` с блоком `backend "s3"`: endpoint на `localhost`, `use_path_style`, отключённые проверки региона и учётных данных, ключ состояния `api/terraform.tfstate`; проверить `tofu fmt -check` и `tofu validate`
- [ ] 4.2 Убедиться, что job `terraform` в `.github/workflows/ci.yml:45` с флагом `-backend=false` продолжает проходить; проверить прогоном на ветке
- [x] 4.3 Описать в `infra/README.md` хранилище состояния, туннель и то, что контейнер MinIO не входит в управляемый стек и не удаляется `tofu destroy`; проверить, что описанная последовательность команд соответствует тому, что делает workflow

## 5. Приёмка

- [ ] 5.1 Прогнать `tofu init` из CI против удалённого состояния и убедиться, что бэкенд принят, а не проигнорирован; проверить по выводу `Successfully configured the backend "s3"`
- [ ] 5.2 Зафиксировать в #22 результат и оставшиеся шаги до CD; проверить, что задача отражает текущее состояние
