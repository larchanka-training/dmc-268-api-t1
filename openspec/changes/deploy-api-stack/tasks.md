# Tasks

## 1. Секреты под стек

- [x] 1.1 Сгенерировать пароли PostgreSQL и RabbitMQ и положить секретами `PG_PASSWORD_DMC268_T1` и `RABBITMQ_PASSWORD_DMC268_T1`; проверить по `gh secret list`, что оба появились
- [x] 1.2 Записать в [#22](https://github.com/larchanka-training/dmc-268-api-t1/issues/22) оговорку из `design.md`: смена пароля на уже созданном томе PostgreSQL не применится, ротация требует отдельной процедуры; проверить, что пункт виден в теле задачи

## 2. Публикация образа

- [ ] 2.1 Добавить job сборки и публикации `ghcr.io/larchanka-training/dmc-268-api` с тегом по commit sha и правами `packages: write`; проверить прогоном, что тег появился в пакетах репозитория
- [ ] 2.2 Убедиться, что образ тянется обратно по этому тегу с правами только на чтение; проверить `docker manifest inspect` из шага прогона

## 3. Terraform: pull вместо сборки

- [x] 3.1 Заменить в `infra/main.tf` блок `build` у `docker_image "api"` на получение готового тега и добавить переменную `api_image` в `infra/variables.tf`; проверить `tofu validate` и что `locals` с хешем исходников удалён, а не осиротел
- [x] 3.2 Добавить `registry_auth` в конфигурацию docker-провайдера с адресом `ghcr.io` и учётными данными из переменных; проверить, что в `infra/` не появилось ни одного значения токена, только переменные
- [x] 3.3 Прокинуть `bind_ip = 0.0.0.0` для API, оставив `internal_bind_ip = 127.0.0.1` для postgres и rabbitmq; проверить в `tofu plan`, что наружу публикуется ровно один порт

## 4. Миграции в Terraform

- [x] 4.1 Добавить `docker_container "migrate"` с `must_run = false`, `attach = true`, тем же образом и командой `alembic upgrade head`, зависящий от postgres; проверить на прогоне, что контейнер завершается с нулевым кодом и Terraform его дожидается
- [x] 4.2 Добавить `depends_on` у `docker_container.api` на контейнер миграций; проверить по `tofu plan`, что порядок создания именно такой
- [ ] 4.3 Убедиться, что упавшая миграция останавливает деплой: проверить на заведомо битой ревизии, что `apply` падает и API не пересоздаётся

## 5. Деплой-workflow

- [x] 5.1 Собрать workflow на `push` в `develop`: публикация образа → хранилище состояния и туннель → `tofu apply` → smoke; проверить, что `tfstate-backend.yml` переиспользуется, а не скопирован
- [x] 5.2 Прокинуть секреты через `TF_VAR_*` и добавить `concurrency`-группу на деплой; проверить, что в `run:` нет интерполяции `secrets.`
- [x] 5.3 Добавить smoke-проверку `/health` через туннель с таймаутом и выводом `docker logs` при неудаче; проверить, что при мёртвом API прогон краснеет и в логе видно причину
- [x] 5.4 Добавить отдельную проверку публичной доступности API по адресу из `VPS_DMC268_IP_T1`; проверить, что она отличает «сервис мёртв» от «порт закрыт снаружи»

## 6. Приёмка

- [ ] 6.1 Выкатить стек полностью с нуля и убедиться по `tofu output`, что `api_url` отвечает; приложить ссылку на прогон в #22
- [ ] 6.2 Повторить деплой без изменений кода и убедиться, что миграции не переприменяются, а контейнеры не пересоздаются
- [ ] 6.3 Обновить `infra/README.md`: откуда берётся образ, как применяются миграции, какие переменные обязательны; проверить, что описанное соответствует workflow
