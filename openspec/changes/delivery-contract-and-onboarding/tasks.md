# Tasks

## 1. Пример переменных Terraform

- [x] 1.1 Дополнить `infra/terraform.tfvars.example` обязательной `api_image` и пояснением, что в CI она и учётные данные реестра приходят через `TF_VAR_*`; проверить, что каждая переменная без `default` в `variables.tf` представлена в примере
- [x] 1.2 Убрать из примера плейсхолдеры, которые в CI не используются, либо подписать, что они только для локального запуска; проверить, что файл не обещает работоспособность, которой нет

## 2. README инфраструктуры

- [x] 2.1 Переписать раздел «Развёртывание и удаление»: локальный `apply` требует туннеля к хранилищу состояния и его учётных данных, а штатный путь — мерж в `develop`; проверить, что в разделе не осталось команд, которые не выполнятся у человека без доступа
- [x] 2.2 Заменить оставшиеся упоминания `terraform` на `tofu` там, где речь о нашей команде запуска; проверить `grep`, что расхождения не осталось

## 3. Переменные приложения

- [x] 3.1 Создать `.env.example` с переменными, которые читает `app/config.py`, и короткой строкой о назначении каждой; проверить, что список совпадает с полями `Settings`
- [x] 3.2 Упомянуть `.env.example` в корневом `README.md` рядом с описанием локального запуска; проверить, что путь запуска описан целиком — от клонирования до отвечающего `/health`

## 4. Приёмка

- [ ] 4.1 Пройти путь нового человека на чистой копии: клонировать, поднять `docker compose`, дождаться `/health`; проверить, что ни одного шага не пришлось доставать из головы вместо документации
- [x] 4.2 Свести спеку с реальностью: убедиться, что `infra/` и `docker-compose.yml` соответствуют каждому требованию `backend-delivery`, и зафиксировать расхождения, если найдутся

## Примечания к выполнению

- **4.1 выполнена частично.** Статическая часть пройдена на чистом клоне: ни одной битой ссылки в `README.md`, `infra/README.md` и `AGENTS.md`, все упомянутые файлы на месте. Сам `docker compose up` не выполнялся — на машине, где шла работа, не запущен демон Docker. Остаётся проверить руками.
- **4.2 прошла, но с оговоркой:** первая версия сверки показала ложное расхождение по миграциям — шаблон поиска не учёл кавычки в объявлении ресурса Terraform. После исправления все пять требований спеки подтверждены обоими описаниями стека. Сверку стоит вынести в CI-проверку отдельной задачей: делать её глазами каждый раз — то же самое, что не делать.
- **design.md не создавался:** артефакт условный, а изменение состоит из документации и фиксации уже достигнутого — архитектурных решений, новых зависимостей и миграционной сложности в нём нет.
