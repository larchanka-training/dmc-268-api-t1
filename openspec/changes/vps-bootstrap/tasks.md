# Tasks

## 1. Ключевая пара CI

- [x] 1.1 Сгенерировать ed25519-пару без пассфразы (CI её не введёт); проверить `ssh-keygen -l -f` — тип `ED25519`, отпечаток совпадает у приватного и публичного файла
- [x] 1.2 Положить приватный ключ секретом `VPS_DMC268_KEY_T1` в `dmc-268-api-t1` через `gh secret set`; проверить, что секрет появился в `gh secret list` и что файлы ключа лежат вне рабочего дерева репозитория
- [x] 1.3 Положить тот же секрет в `dmc-268-ui-t1` (сервер общий, секреты — нет); проверить `gh secret list` для этого репозитория
- [x] 1.4 Записать в [#22](https://github.com/larchanka-training/dmc-268-api-t1/issues/22), что ключ заведён в двух местах и при ротации меняется в обоих; проверить, что пункт виден в теле задачи

## 2. Скрипт подготовки сервера

- [x] 2.1 Написать чистую функцию `render_bootstrap_script` (вход — имя пользователя и публичный ключ, выход — текст bash-скрипта, без сети и без секретов внутри); проверить локально: на синтетических значениях вывод содержит ожидаемые проверки и не содержит слова `SSHPASS`
- [x] 2.2 Реализовать в скрипте определение прав — `root`, `sudo` без пароля, `sudo` с паролем со stdin, либо отсутствие прав; проверить, что четвёртый исход завершает скрипт с понятным сообщением и не пытается ставить пакеты
- [x] 2.3 Реализовать идемпотентную установку Docker через `get.docker.com`; проверить, что при уже установленном Docker шаг сообщает «уже установлен» и не запускает скрипт установки
- [x] 2.4 Реализовать идемпотентное добавление пользователя в группу `docker` по `id -nG`; проверить, что повторный вызов сообщает «уже в группе»
- [x] 2.5 Реализовать идемпотентную запись публичного ключа в `authorized_keys` точным совпадением строки, с правами `700` на `~/.ssh` и `600` на файл; проверить локально на временном каталоге, что второй вызов не добавляет вторую строку

## 3. Workflow

- [x] 3.1 Собрать `workflow_dispatch`-workflow со входом `public_key`, выполняющий скрипт одной сессией `sshpass -e ssh ... bash -s`; проверить, что в YAML нет интерполяции `secrets.` внутри `run:` и что вызов `ssh` в файле ровно один
- [x] 3.2 Вывести в summary исходы всех четырёх шагов (права, Docker, группа, ключ); проверить, что отсутствие прав даёт зелёный прогон с явным сообщением, а не падение

## 4. Прогон и приёмка

- [ ] 4.1 Запустить bootstrap и убедиться по summary, что Docker установлен, пользователь в группе, ключ добавлен
- [ ] 4.2 Прогнать `vps-access-probe` повторно и убедиться, что он показывает «Docker: ок»; это же исполнит его ветку входа по ключу, оставшуюся непроверенной
- [ ] 4.3 Зафиксировать результат в #22 и удалить оба временных workflow вместе со скриптами — закрывает задачу 5.3 изменения `vps-access-probe`; проверить, что в `.github/workflows/` остался только `ci.yml`
