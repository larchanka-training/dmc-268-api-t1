# Tasks

## 1. Бамп

- [x] 1.1 Поднять `tofu_version` до 1.12.6 в `ci.yml`, `deploy.yml` и `tfstate-backend.yml`; проверить, что ни одного упоминания 1.10.5 не осталось
- [x] 1.2 Поднять `required_version` в `infra/versions.tf` до `>= 1.12.0` и поправить упоминание версии в `infra/README.md`; проверить, что README и конфигурация называют одну и ту же версию

## 2. Приёмка

- [ ] 2.1 Дождаться зелёного job `terraform` в `ci.yml` на новой версии (`fmt -check`, `init -backend=false`, `validate`); приложить ссылку на прогон
- [ ] 2.2 Прогнать деплой и убедиться, что `apply` на существующем состоянии даёт `No changes` — то есть новая версия не считает конфигурацию изменившейся
- [ ] 2.3 Убедиться, что стенд продолжает отвечать по `/health` после прогона
